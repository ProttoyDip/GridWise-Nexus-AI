"""Deterministic normalization of LLM-extracted values.

The LLM extracts natural-language expressions; the normalizer converts them
into exact machine-checkable values. The LLM alone never decides final hours.
"""

